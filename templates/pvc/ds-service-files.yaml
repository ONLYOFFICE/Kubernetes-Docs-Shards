{{- if eq (include "ds.pvc.create" .) "true" }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ds-service-files
  namespace: {{ include "ds.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
    {{- include "ds.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.persistence.dsServiceFiles.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.persistence.dsServiceFiles.annotations }}
  annotations: {{- include "ds.annotations.commonAnnotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  storageClassName: {{ .Values.persistence.dsServiceFiles.storageClass }}
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.persistence.dsServiceFiles.size }}
{{- end }}
